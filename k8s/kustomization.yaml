apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app: doc-system

namespace: default

# Optionally add ConfigMaps or Secrets
# configMapGenerator:
# - name: doc-system-config
#   files:
#   - config.json
